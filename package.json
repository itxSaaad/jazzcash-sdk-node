{
  "name": "jazzcash-sdk-node",
  "version": "1.0.1",
  "description": "A comprehensive Node.js package for integrating the JazzCash Payment Gateway. Supports Card, Mobile Account, Voucher, and 3D Secure APIs with support for API versions 1.x and 2.x.",
  "main": "src/index.js",
  "scripts": {
    "release:patch": "npm version patch && git push --follow-tags && npm publish && gh release create \"$(git describe --tags)\" --generate-notes --notes-start-tag \"$(git describe --tags HEAD^)\"",
    "release:minor": "npm version minor && git push --follow-tags && npm publish && gh release create \"$(git describe --tags)\" --generate-notes --notes-start-tag \"$(git describe --tags HEAD^)\"",
    "release:major": "npm version major && git push --follow-tags && npm publish && gh release create \"$(git describe --tags)\" --generate-notes --notes-start-tag \"$(git describe --tags HEAD^)\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/itxSaaad/jazzcash-sdk-node.git"
  },
  "keywords": [
    "jazzcash",
    "payment",
    "gateway",
    "nodejs",
    "integration",
    "api",
    "finance"
  ],
  "author": "Muhammad Saad",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/itxSaaad/jazzcash-sdk-node/issues"
  },
  "homepage": "https://github.com/itxSaaad/jazzcash-sdk-node#readme",
  "dependencies": {
    "axios": "^1.8.2",
    "dotenv": "^16.4.7"
  }
}
